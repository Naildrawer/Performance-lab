Seq Scan (Sequential Scan) - PostgreSQL читает всю таблицу подряд, строка за строкой
Index Scan — PostgreSQL сначала смотрит в индекс, а потом находит нужные строки в таблице.
EXPLAIN ANALYZE — показывает, как именно PostgreSQL выполнил запрос и сколько времени
